<% @categories.map do |parent| %>
  <p><%= parent.name %></p>
  <ul>
    <% parent.children.map do |child| %>
      <li><%= link_to child.name, params.merge(category_id:child.id) %></li>
    <% end %>
  </ul>
<% end %>